# Bee My Honey — 開発マイルストーン

このドキュメントは、過去のリリースノート/チェンジログと今後の実装計画を1つにまとめたものです。
セッションをまたいで開発を引き継ぐ際は、`CONVENTIONS.md` と合わせて最初に目を通してください。

---

## 現在の優先順位

次のアプデに着手する前に、この順番で進める:

1. ~~**コンベンション適用 / 翻訳キー一元化**~~ — v0.3.3で完了。直書きされていた文言を全て`strings.js`に寄せ、`t(lang, STR.xxx)`経由に統一した
2. ~~**UI関連バグ潰し**~~ — v0.3.3で完了。`check_undefined.js`の警告は実質ゼロ(残る`economy/bank.js`の`giveItemJob`警告はジェネレータ関数の誤検出のみ)
3. **テンプレート作り**(次はここから) — 1・2でコードベースの見通しが良くなった状態で、「テンプレート設計」セクション(下記)のテンプレート群を書き出す
4. その後に「取引・クエスト・人材派遣を貫くP2P共通基盤」「NPCが自律的に経済活動を続ける仕組み」に着手

---

## これまでの経緯(Changelog)

### v0.1.1 — 初期配布
- 初回リリース。為替5種・株式5種・花の先物(単一契約)・コールドウォレット(ストーリーコード方式)を実装。
- readmeに日本語訳リンクを追加。

### v0.2.2("v2.2") — 為替拡張・行商人
- 外貨為替システムを拡張。
- 行商人(wandering trader)の巡回に責任を持たせる形の仕組みを追加(`scripts/trader/wandering-trader.js`)。

### v0.3.0 — 融資・保険・通販・先物拡張
**New**
- 融資システム(5段階、信用スコア連動の金利・審査)
- 信用スコア制度(300〜850。融資・クレカ・保険・破産処理で共通利用)
- 保険(海運・漁獲・海底資産)。骨ブロック+植木鉢で開設
- 郵便販売(Skein)+クレジットカード。書見台+紙のカタログショップ、リボ払い、日替わり特売、Prime的サブスク
- 花の先物を単一契約→5銘柄(Rose/Allium/Jade Orchid/Sunflower/Sakura)に拡張。複数ロット・株式と同じ週間パターン連動の価格付け
- 銀行UI刷新(+/-数量モーダル、融資デスクへの導線)
- 新規株式2銘柄: Skein(SKN)、KelpLife Holdings(KLH。現金配当型)
- 現金配当(株式配当がエメラルド直接入金に対応)
- アイテムカタログモジュール新設(苗木・色違いなどのバリアント系アイテムを一元管理)

**Improvements**
- ダイヤ/金/ラピスブロックが所持数換算に対応(1ブロック=9単位)
- ゴーレム・モブトレードの購入品を足元ドロップに統一
- 価格変動をサインカーブ→4種のシャッフル済み週間パターンテーブルに変更(株式・為替・先物で共通化)
- エンチャント済みアイテムの付与に対応(郵便販売の書籍カテゴリで使用)
- 週次リセット処理に融資返済・カード請求・保険料徴収を統合

**Other**
- ローカライズ文言を約60件追加/更新
- manifestバージョンを0.2.2→0.3.0に

### v0.3.1 — セキュリティ修正・労働市場・需要圧力(実装済み)
**Security / Fixed**
- コールドウォレットの署名偽造脆弱性を修正。ハードコードsaltを廃止し、ワールド固有のランダムシークレット方式に変更。別ワールド持ち出しはオペレーター限定のオプション機能に格下げ
- `giveItem`の大量ドロップによるサーバー負荷を修正(`system.runJob`でtick分割、取引上限キャップ追加)
- 融資の満期利息計算バグを修正(契約時の週数を保存せず常に1週固定になっていた問題)
- `manifest.json`の未使用`script_eval`権限を削除

**Added**
- 労働市場「HRMHRM Partners HLD」を新設(書見台+本)
  - スタッフサービス(日替わりクエスト、コンテナ納品方式)
  - ライセンス制度(実測進捗方式。ネザー/水中/エンドの対象ブロック設置・破壊実績でLv1→Lv2)
  - コンサルタントサービス(村人の貸与契約)
  - オーナーズクラブ(村人を履歴書データとして登録・派遣。128件キャップ、100件超で事故率上昇)
  - 履歴書テンプレート(`workers_resume`)、クエスト共通テンプレート(`quest_listing`。会社機能を見据えた設計)
  - 人件費の統一シンク(`LABOR_COST_PER_DAY`)
- 需要圧力による相場変動(取引所を通った売買量に応じて価格が±60%まで変動、日次減衰)。為替・株式・現物資産取引に適用
- HRMHRM株(HRM)と、労働市場での仕事発生ごとに自社株が付与される新配当型(`dividendType: "stock_option"`)

**Changed**
- ゴーレムとの現物資産取引を、独自ドロップ実装から共通`giveItem`に統一

### v0.3.3 — コンベンション適用・i18n一元化(安定版、0.3.2は内部スナップショット)
**Refactor**
- `CONVENTIONS.md`の規約に沿って、労働市場・保険・先物・郵便販売・ウォレット管理者設定・ゴーレム取引メッセージなど各所に残っていた直書き文言(インラインの`{ja:..., en:...}`や`labor-menu.js`独自のローカル関数`L()`)を全て`scripts/i18n/strings.js`の`STR`オブジェクトに集約し、`t(lang, STR.xxx)`経由の呼び出しに統一
- `labor-menu.js`のローカル関数`L()`を廃止(CONVENTIONS.md内で「規約違反の残骸」と明記されていたものを解消)
- 労働市場のクエスト納品物/ライセンス対象ブロック・バイオームの表示名を`LABOR_ITEM_NAMES`/`LABOR_BIOME_NAMES`として`labor-data.js`に一元化
- 株式市場ボタンの銘柄数表示を「7銘柄」固定書きから`Object.keys(STOCKS).length`を使った動的表示に変更
- `main-menu.js`を`trading-menu.js`にリネーム(`openMainMenu`→`openTradingMenu`)。呼び出し元(`main.js`・`futures-menu.js`・`stock-menu.js`等)を追従修正
- 未使用だったP2P基盤スタブ`scripts/data/market-listing-data.js`を削除(どこからも参照されていなかった)

**Fixed**
- `check_undefined.js`が検出していた`ui/stock-menu.js`の`L()`未定義参照を解消。残る`economy/bank.js`の`giveItemJob`警告はジェネレータ関数の誤検出のみ(実害なし)で、実質警告ゼロを達成
- 株価アイコン(HRMHRM株・ひまわり先物)の色コードを`§e`(黄)から`§6`(金)に変更し、他の黄色系アイコンと区別しやすく調整

**Other**
- `manifest.json`の未使用`script_eval`権限を実際に削除(v0.3.1のchangelog記載時点ではまだ反映されていなかったものを本リリースで反映)
- manifestバージョンを0.3.1→0.3.3に更新

---

## 今後のマイルストーン(計画)

### 全体方針
現状のマイルストーンは基本的に「1プレイヤー対システム(HRMHRM)」の設計で、マルチプレイ要素(プレイヤー間の直接取引)がまだない。今後は、現状HRMHRMが仲介しているアイテム/お金の取引・クエスト・人材派遣を、すべてプレイヤー同士でも機能させたい。また、ソロプレイでも村人が経済の一員であることが実感できるようにしたい。そのため次の大型アップデートは、単発の「会社」機能というより以下の2本柱として捉える。

### 柱1: 取引・クエスト・人材派遣を貫くP2P共通基盤
- `quest_listing`(issuer/requirement/reward/status/claimedBy)は0.3.1で先取り実装済み。issuerを`company`/`player`に広げる形で流用予定
- 現状バラバラの3実装(取引=通販、クエスト=労働市場、人材派遣=オーナーズクラブ)を、共通の「発注/受注」骨格に寄せる
- 郵便販売の取引所化: HRMHRMが常時規定数を高めの価格で出品しつつ、プレイヤーも出品してマージンを稼げるようにする構想。「HRMHRMは放置していても村人が自動消化して稼ぐが、プレイヤーが介入して初めて価値が世界に循環する」という設計思想
- プレイヤーによる株式上場もこの延長線上

### 柱2: NPCが自律的に経済活動を続ける仕組み
- HRMHRMの自社口座(資本金)構想。村人・HRMHRMがバックグラウンドで勝手に経済活動を続け(誰も見てなくても仕事を消化する、口座残高が動く)、ソロプレイでも経済の一員であることが伝わるようにする

### テンプレート設計(柱1・2の前提タスク)
上記2本柱に着手する前に、下記のテンプレートを書き出す。これがないと村人が動けない。

- **`workers_resume`(既存)** — 村人・プレイヤー共通の履歴書。「NPCが自律的に働く」を入れるなら、現状の受動的なフィールド(records/skills/licenses)だけで足りるか要検討
- **`quest_listing`(既存、拡張要)** — issuerを`player`/`company`に開放する部分の詳細仕様がまだない
- **取引/出品テンプレート(未定義)** — 通販・取引所でのアイテム出品用。`quest_listing`と統合するか別建てにするか未決定
- **口座テンプレート(未定義)** — HRMHRM自身や将来の「会社」が持つ資本金・収支を表す型。個人プレイヤーの口座(既存のwallet)とは別に必要
- **人材派遣契約テンプレート(未定義)** — オーナーズクラブの「派遣中」状態は今`workers_resume`側の運用フィールド(status/dispatchEndDay等)に間借りしている状態なので、独立した契約テンプレートにするか判断が必要

### ライセンス制度の拡張
- 現状はLv1(見習い)/Lv2(一人前)のみ実装
- `stages`配列でデータ駆動設計済みのため、Lv3以降(複数チャンクにまたがるスケールアウト等)は後から追加可能な状態

### その他の積み残し・簡略化ポイント
- コンサルタントサービスの職業ブロック連携(今回はスポーンエッグ+ベッドのみ)
- 需要圧力の花の先物への適用(理論価格が別建て指標のため設計再検討が必要)
- 最寄りネザーゲート探索の範囲拡張・手動登録対応

### 見送り事項
- 株連動型敵対解除システム: 他MODとの機能被りを避けるため対応なし
- `team`系コマンド: Bedrockには存在しないため、実装する場合は`family`コンポーネントや`entity.triggerEvent`ベースでの再設計が必要(要調査)

---

## 今回のセッションについて

今回はコードの変更は行っていません。v0.3.1→v0.3.3(安定版)の差分をリリースノートとして本ファイルに反映し、優先順位1・2の完了を記録しただけです。次にこのプロジェクトを触るセッションは、`CONVENTIONS.md`を読んだ上で、優先順位3「テンプレート作り」(下記「テンプレート設計」セクション)から着手してください。
